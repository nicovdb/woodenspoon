<div class="container">
  <%= simple_form_for(@team) do |f| %>
    <%= f.input :name, label: 'Nom'%>
    <%= f.input :description %>
    <%= f.submit "Créer l'équipe", class: 'btn btn-success' %>
  <% end %>
</div>
